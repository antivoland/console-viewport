package antivoland.console.viewport.view.export;

import antivoland.console.viewport.tick.Tick;
import antivoland.console.viewport.view.Aging;

public class GifRecorder extends Aging {
    private static final int DEFAULT_FRAME_INTERVAL_MILLIS = 100;

    @Override
    public void tick(Tick tick) {
        super.tick(tick);

    }
}